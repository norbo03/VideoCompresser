# VideoCompresser
A custom bash script that compresses videos with the use of ffmpeg.
It halves the video resolution (keeping the aspect ratio) and sets the video bitrate to 5000 kbits/sec.

# Usage: 
```bash
./compress.sh -i <input_dir> -o <output_dir>
```
